# オシドリ v6.1 全面改修計画

**作成日:** 2026年3月7日
**前提:** 事業計画書v6.1「AI共感CRM統合版」＋仕様変更書v6.1対応版に基づく
**現状:** Phase 1完了（MVP全実装）、テスト整備完了（Vitest 61テスト + Playwright E2E 3スイート）

---

## 1. 現状分析

### 1-1. 実装済み機能（v2.x系）

| カテゴリ | 実装内容 | v6.1対応状況 |
|---------|---------|-------------|
| **AI CM提案** | ✅ 021マイグレーション完了、ai_cm_proposalsテーブル、visit_milestonesテーブル | 🟡 DBは完成、フロントUIが未実装 |
| **気分タグ** | ✅ 015マイグレーション完了、mood_tags配列カラム追加 | 🟡 DB完成、消費者UI未実装 |
| **インタビュー** | ✅ 4タイプ実装済み（初回/月次/メニュー/季節） | 🟡 30分化への調整が必要 |
| **メニュー** | ✅ menusテーブル実装済み、写真・価格カラム存在 | ✅ v6.1仕様を満たしている |
| **チェックイン** | ✅ visit_recordsテーブル実装済み | 🔴 推し登録統合フローが未実装 |
| **課金機能** | 🔴 Stripe連携未実装（外部要因依存） | ✅ v6.1で完全削除のため対応不要 |
| **推しスコア** | 🔴 実装なし | ✅ v6.1で排除のため対応不要 |
| **SNS配信** | 🔴 手動配信のみ | 🔴 自動配信への変更が必要 |

### 1-2. v6.1で新規に必要な機能

| 機能 | 優先度 | 実装規模 | 備考 |
|-----|-------|---------|------|
| AI CM提案カード（店舗ダッシュボード） | MVP必須 | 大 | DBは完成、UI実装が必要 |
| パーソナルレコメンド（消費者ホーム） | MVP必須 | 大 | 気分タグベースのレコメンド |
| 推し店マップ | MVP必須 | 中 | 地図表示＋位置情報連携 |
| 気分タグUI（消費者） | MVP必須 | 中 | タグ選択・保存・フィルタ |
| チェックイン=推し登録統合フロー | MVP必須 | 中 | ワンタップで両方完了 |
| QRコード/POP生成 | MVP必須 | 中 | チェックイン導線 |
| 体験記録・ファンレター表示（店舗側） | MVP必須 | 小 | 既存データの表示強化 |
| 予約打診UI強化 | MVP必須 | 小 | 既存機能の導線改善 |
| SNS自動配信 | P1 | 中 | AI CMによる自動配信 |
| 位置情報リマインド | P1 | 中 | Geofencing |
| 滞在時間サジェスト | P1 | 中 | バックグラウンド位置情報 |

---

## 2. v6.1改修の優先順位付け

### Phase 1: MVP必須機能（推定10-14日）

#### P1-1: AI CMダッシュボード実装（3-4日）
- [ ] `/dashboard` ホーム画面にAI CM提案カード追加
- [ ] GET `/api/cm-proposals` 提案一覧API（既存src/lib/ai/cm-proposals.ts活用）
- [ ] POST `/api/cm-proposals/[id]/respond` 承認/却下API
- [ ] AI CM提案カードコンポーネント開発
- [ ] 提案承認時のアクション実行（メッセージ送信等）

#### P1-2: 消費者ホーム画面再設計（3-4日）
- [ ] パーソナルレコメンド機能実装
  - [ ] GET `/api/recommendations/personal` API
  - [ ] 気分タグ × 店舗データのマッチングロジック
  - [ ] shop_mood_scoresテーブル作成（022マイグレーション）
- [ ] 今日のおすすめ＋旬の食材アラート統合表示
- [ ] 推し店新着更新セクション

#### P1-3: 気分タグシステム実装（2-3日）
- [ ] consumer_mood_preferencesテーブル作成（022マイグレーション）
- [ ] 気分タグ選択UI（マイページ設定）
- [ ] 探す画面に気分タグフィルター追加
- [ ] 体験記録の感情タグ保存・表示

#### P1-4: 推し店マップ実装（2-3日）
- [ ] `/oshi-map` ページ作成
- [ ] Google Maps API連携（またはLeaflet等のOSS地図）
- [ ] shopsテーブルにlatitude/longitude追加（022マイグレーション）
- [ ] マップ/リスト切り替えUI
- [ ] シェア用URL生成

#### P1-5: チェックイン統合フロー実装（2日）
- [ ] チェックイン画面を同時フロー化
- [ ] PUT `/api/oshi` と POST `/api/visits` の統合実行
- [ ] QRコード生成機能
  - [ ] shop_qr_codesテーブル作成（022マイグレーション）
  - [ ] GET `/api/shops/[id]/qr-codes` API
  - [ ] POP画像生成（next/imageで動的生成）

#### P1-6: AIインタビュー30分化（1-2日）
- [ ] src/lib/prompts/interview-system.tsのフェーズ時間調整
- [ ] インタビューUI進捗バーの調整
- [ ] メニューインタビューの10分フロー確認

### Phase 2: 体験改善・P1機能（推定5-7日）

#### P2-1: 店舗ダッシュボード強化（2日）
- [ ] ストーリー閲覧者数カウント追加
- [ ] 体験記録・ファンレター一覧表示強化
- [ ] 来店回数・段階表示（visit_milestones活用）

#### P2-2: SNS自動配信（2-3日）
- [ ] sns_auto_publish_configテーブル作成（023マイグレーション）
- [ ] AI CMによる最適配信タイミング判定
- [ ] Instagram/X/Facebook API連携（外部連携フレームワーク拡張）

#### P2-3: 位置情報関連機能（2-3日）
- [ ] Geofencing実装（react-native-geolocation-service等）
- [ ] 位置情報リマインドプッシュ通知
- [ ] 滞在時間検知＋チェックインサジェスト

### Phase 3: クリーンアップ（推定3-4日）

#### P3-1: 課金機能コード削除
- [ ] 応援チップ関連コンポーネント削除
- [ ] 応援メンバー関連コンポーネント削除
- [ ] 物販関連コンポーネント削除
- [ ] Stripe関連未使用コード削除

#### P3-2: 推しスコア排除
- [ ] 推しスコア表示コンポーネント削除
- [ ] 関連ロジック削除

#### P3-3: UI/UX「3タップ以内」ルール適用
- [ ] プログレッシブ開示の実装確認
- [ ] 主要動線の見直し
- [ ] 操作回数の検証

---

## 3. DBマイグレーション計画

### 022_v6.1_core_features.sql（Phase 1対応）

```sql
-- 店舗の位置情報（推し店マップ用）
ALTER TABLE shops ADD COLUMN IF NOT EXISTS latitude DECIMAL(10, 8);
ALTER TABLE shops ADD COLUMN IF NOT EXISTS longitude DECIMAL(11, 8);
ALTER TABLE shops ADD COLUMN IF NOT EXISTS geofence_radius INTEGER DEFAULT 50;

-- 消費者の気分タグ設定
CREATE TABLE IF NOT EXISTS consumer_mood_preferences (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  consumer_id uuid NOT NULL REFERENCES users(id),
  mood_tags text[] NOT NULL DEFAULT '{}',
  updated_at timestamptz NOT NULL DEFAULT now(),
  UNIQUE(consumer_id)
);

-- 店舗の気分マッチングスコア（AIが自動算出）
CREATE TABLE IF NOT EXISTS shop_mood_scores (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  shop_id uuid NOT NULL REFERENCES shops(id) ON DELETE CASCADE,
  mood_tag text NOT NULL CHECK (mood_tag IN (
    'relaxed', 'energetic', 'reward', 'social', 'solo',
    'discovery', 'comfort', 'celebration'
  )),
  score decimal(3,2) NOT NULL DEFAULT 0.00 CHECK (score >= 0 AND score <= 1),
  sample_count integer NOT NULL DEFAULT 0,
  updated_at timestamptz NOT NULL DEFAULT now(),
  UNIQUE(shop_id, mood_tag)
);

-- QRコード管理
CREATE TABLE IF NOT EXISTS shop_qr_codes (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  shop_id uuid NOT NULL REFERENCES shops(id) ON DELETE CASCADE,
  qr_type text NOT NULL CHECK (qr_type IN ('checkin', 'story', 'menu')),
  qr_url text NOT NULL,
  design_template text DEFAULT 'default',
  created_at timestamptz NOT NULL DEFAULT now()
);

-- チェックイン方式の追加
ALTER TABLE visit_records ADD COLUMN IF NOT EXISTS method text DEFAULT 'qr'
  CHECK (method IN ('qr', 'location_remind', 'dwell_time'));

-- インデックス
CREATE INDEX IF NOT EXISTS idx_consumer_mood_prefs_consumer_id ON consumer_mood_preferences(consumer_id);
CREATE INDEX IF NOT EXISTS idx_shop_mood_scores_shop_id ON shop_mood_scores(shop_id);
CREATE INDEX IF NOT EXISTS idx_shop_qr_codes_shop_id ON shop_qr_codes(shop_id);

-- RLS
ALTER TABLE consumer_mood_preferences ENABLE ROW LEVEL SECURITY;
ALTER TABLE shop_mood_scores ENABLE ROW LEVEL SECURITY;
ALTER TABLE shop_qr_codes ENABLE ROW LEVEL SECURITY;

-- RLSポリシー（consumer_mood_preferences）
CREATE POLICY "mood_prefs_user_all" ON consumer_mood_preferences
  FOR ALL USING (consumer_id = auth.uid());

-- RLSポリシー（shop_mood_scores）
CREATE POLICY "mood_scores_public_read" ON shop_mood_scores
  FOR SELECT USING (true);

CREATE POLICY "mood_scores_admin_all" ON shop_mood_scores
  FOR ALL USING ((SELECT role FROM users WHERE id = auth.uid()) = 'admin');

-- RLSポリシー（shop_qr_codes）
CREATE POLICY "qr_codes_owner_all" ON shop_qr_codes
  FOR ALL USING (shop_id IN (SELECT id FROM shops WHERE owner_id = auth.uid()));

CREATE POLICY "qr_codes_public_read" ON shop_qr_codes
  FOR SELECT USING (true);
```

### 023_sns_auto_publish.sql（Phase 2対応）

```sql
-- SNS自動配信設定
CREATE TABLE IF NOT EXISTS sns_auto_publish_config (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  shop_id uuid NOT NULL REFERENCES shops(id) ON DELETE CASCADE,
  platforms text[] NOT NULL DEFAULT '{}', -- ['instagram', 'x', 'facebook']
  auto_publish boolean NOT NULL DEFAULT false,
  optimal_timing jsonb DEFAULT '{}', -- AI判定の最適配信時間
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now(),
  UNIQUE(shop_id)
);

CREATE INDEX IF NOT EXISTS idx_sns_config_shop_id ON sns_auto_publish_config(shop_id);

ALTER TABLE sns_auto_publish_config ENABLE ROW LEVEL SECURITY;

CREATE POLICY "sns_config_owner_all" ON sns_auto_publish_config
  FOR ALL USING (shop_id IN (SELECT id FROM shops WHERE owner_id = auth.uid()));
```

---

## 4. 実装スケジュール

```
Week 1: Phase 1-1〜1-3 (AI CM + 消費者ホーム + 気分タグ)
Week 2: Phase 1-4〜1-6 (推し店マップ + チェックイン統合 + インタビュー30分化)
Week 3: Phase 2 (店舗ダッシュボード強化 + SNS自動配信 + 位置情報)
Week 4: Phase 3 (クリーンアップ + テスト) + ユーザーテスト準備
```

**合計推定:** 18-25日（3-4週間）

---

## 5. テスト戦略

### 5-1. 新規実装のテストカバレッジ

| 機能 | E2Eテスト | APIテスト | コンポーネントテスト |
|-----|---------|---------|------------------|
| AI CM提案カード | ✅ 必須 | ✅ 必須 | ✅ 必須 |
| パーソナルレコメンド | ✅ 必須 | ✅ 必須 | ✅ 必須 |
| 推し店マップ | ✅ 必須 | ✅ 必須 | ✅ 必須 |
| 気分タグUI | ✅ 必須 | ✅ 必須 | ✅ 必須 |
| チェックイン統合 | ✅ 必須 | ✅ 必須 | - |
| QRコード生成 | - | ✅ 必須 | ✅ 必須 |

### 5-2. 既存機能のリグレッションテスト

- [ ] 全61テスト + E2E 3スイートが引き続きパスすることを確認
- [ ] 新規マイグレーション適用後のRLSポリシー動作確認
- [ ] パフォーマンステスト（レコメンドAPI応答時間）

---

## 6. リスク・懸念事項

| リスク | 影響 | 対策 |
|-------|------|------|
| 位置情報機能のプライバシー懸念 | 高 | ユーザー明示的同意UI、プライバシーポリシー更新 |
| Google Maps API料金 | 中 | 無料枠内での運用設計、OSS地図ライブラリの検討 |
| AI CMの提案品質 | 中 | フィードバックループ構築、店主の編集可能性確保 |
| SNS API連携の複雑性 | 中 | フェーズ分割、最初はInstagramのみ等 |
| パーソナルレコメンドのコールドスタート問題 | 低 | デフォルトレコメンド設計、人気店舗の優先表示 |

---

## 7. 成功指標（PMF測定）

v6.1改修完了後、以下の指標でPMFを測定（事業計画書9-2c参照）：

| 軸 | 測定指標 | 目標値（PoC） |
|---|---------|-------------|
| **Must-Have度** | 「もしこのサービスがなくなったら困る」率 | 40%以上 |
| **Retention** | 月次継続率（店舗） | 90%以上 |
| **Engagement** | 週次AIインタビュー完遂率 | 70%以上 |
| **NPS** | 推奨度（店舗） | +50以上 |
| **Activation** | 初回インタビュー→ストーリー公開率 | 80%以上 |

---

## 8. Next Actions（即着手項目）

1. ✅ v6.1改修計画の策定（このドキュメント）
2. ⏭ 022マイグレーション作成・適用
3. ⏭ AI CMダッシュボード実装開始
4. ⏭ 消費者ホーム画面再設計開始
5. ⏭ 気分タグシステム実装開始

---

*以上 — オシドリ v6.1 全面改修計画*
